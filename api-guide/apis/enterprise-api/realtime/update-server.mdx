---
title: "Enterprise: Update Server Endpoint"
description: "This endpoint is used to update your dedicated open server."
openapi: "POST /realtime/update_server"
---

## Request
Make a `POST` request to below endpoint and pass the required parameters as a request body.
```curl curl
--request POST 'https://modelslab.com/api/v1/enterprise/realtime/update_server' \
```

## Body
```json json
{ 
    "key": "enterprise_api_key"
}
```