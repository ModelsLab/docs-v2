---
title: "Enterprise: Clear API Cache Endpoint"
description: "This endpoint is used to clear the API's cache."
openapi: "POST /voice-cloning/clear_cache"
---

## Request
Send a `POST` request to below endpoint to clear the API's cache.
```curl curl
--request POST 'https://modelslab.com/api/v1/enterprise/voice/clear_cache' \
```

## Body
```json json
{ 
    "key": "enterprise_api_key"
}
```