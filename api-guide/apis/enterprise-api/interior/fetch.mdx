---
title: "Enterprise: Fetch Queued Request Endpoint"
description: "Fetch Queued Request API fetches queued request."
openapi: "POST /interior/fetch/{id}"
---

## Request
Send a `POST` request to below endpoint to return the corresponding queued images.
```curl curl 
--request POST 'https://modelslab.com/api/v1/enterprise/interior/fetch/{id}' \
```

## Body
```json json
{ 
    "key": "enterprise_api_key"
}
```