---
title: "Text to Image"
openapi: 'POST /text2img'
description: 'Create high quality images from text using the NanoBanana Lite Pretrained model.'
---

Generate images from text descriptions using the NanoBanana Lite Pretrained model. This model delivers fast, high-quality image generation with excellent prompt adherence.

## Request

Make a `POST` request to the endpoint below with the required parameters.

```bash
POST https://modelslab.com/api/v6/images/text2img
```

## Request Example

<CodeGroup>

```json Body
{
    "key": "your_api_key",
    "model_id": "nanobanana-lite-pretrained",
    "prompt": "ultra realistic close up portrait ((beautiful pale cyberpunk female with heavy black eyeliner)), blue eyes, shaved side haircut, hyper detail, cinematic lighting, magic neon, dark red city, Canon EOS R3, nikon, f/1.4, ISO 200, 1/160s, 8K, RAW, unedited, symmetrical balance, in-frame, 8K",
    "negative_prompt": "blurry, low quality, distorted, deformed",
    "width": 512,
    "height": 512,
    "samples": 1,
    "num_inference_steps": 31,
    "safety_checker": "no",
    "seed": null,
    "guidance_scale": 7.5,
    "clip_skip": 2,
    "webhook": null,
    "track_id": null
}
```

```python Python
import requests

response = requests.post(
    "https://modelslab.com/api/v6/images/text2img",
    json={
        "key": "your_api_key",
        "model_id": "nanobanana-lite-pretrained",
        "prompt": "ultra realistic portrait of a cyberpunk woman, blue eyes, cinematic lighting, 8K",
        "negative_prompt": "blurry, low quality, distorted",
        "width": 512,
        "height": 512,
        "samples": 1,
        "num_inference_steps": 31,
        "guidance_scale": 7.5
    }
)

data = response.json()
print(data)
```

```javascript JavaScript
const response = await fetch("https://modelslab.com/api/v6/images/text2img", {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({
    key: "your_api_key",
    model_id: "nanobanana-lite-pretrained",
    prompt: "ultra realistic portrait of a cyberpunk woman, blue eyes, cinematic lighting, 8K",
    negative_prompt: "blurry, low quality, distorted",
    width: 512,
    height: 512,
    samples: 1,
    num_inference_steps: 31,
    guidance_scale: 7.5
  })
});

const data = await response.json();
console.log(data);
```

```bash cURL
curl -X POST "https://modelslab.com/api/v6/images/text2img" \
  -H "Content-Type: application/json" \
  -d '{
    "key": "your_api_key",
    "model_id": "nanobanana-lite-pretrained",
    "prompt": "ultra realistic portrait of a cyberpunk woman, blue eyes, cinematic lighting, 8K",
    "negative_prompt": "blurry, low quality, distorted",
    "width": 512,
    "height": 512,
    "samples": 1,
    "num_inference_steps": 31,
    "guidance_scale": 7.5
  }'
```

</CodeGroup>

<Tip>
When you receive a processing response, use the [Fetch endpoint](/image-generation/community-models/dreambooth-fetch-queue-image) to poll for results.
</Tip>

## Related Endpoints

<CardGroup cols={2}>
  <Card title="Image to Image" icon="image" href="/image-generation/nanobanana-lite-pretrained/image-to-image">
    Transform existing images with NanoBanana Lite
  </Card>
  <Card title="Fetch Results" icon="download" href="/image-generation/community-models/dreambooth-fetch-queue-image">
    Retrieve results for processing requests
  </Card>
</CardGroup>
