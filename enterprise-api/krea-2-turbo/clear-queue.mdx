---
title: "Clear Queue"
description: "This endpoint is used to clear the API's queue."
openapi: "POST /krea_2_turbo/clear_queue"
---

## Request
Send a `POST` request to below endpoint to clear the API's queue.
```curl curl
--request POST 'https://modelslab.com/api/v1/enterprise/krea_2_turbo/clear_queue' \
```

## Body
```json json
{ 
    "key": "enterprise_api_key"
}
```
