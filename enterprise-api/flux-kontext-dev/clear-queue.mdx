---
title: "Clear Queue"
description: "This endpoint is used to clear the API's queue."
openapi: "POST /enterprise/flux_kontext_dev/clear_queue"
---

## Request
Send a `POST` request to below endpoint to clear the API's cache.
```curl curl
--request POST 'https://modelslab.com/api/v1/enterprise/flux_kontext_dev/clear_queue' \
```

## Body
```json json
{ 
    "key": "enterprise_api_key"
}
```